// Configuración de PRODUCCIÓN (build con `ng build`, configuración por defecto).
//
// IMPORTANTE: reemplazar la URL por la del backend desplegado en Render una vez se conozca.
// Debe terminar en /api y NO llevar barra final.
// Ejemplo final: https://chemicallab-backend.onrender.com/api
export const environment = {
  production: true,
  apiUrl: 'https://chemicallab-backend.onrender.com/api'
};
